import "./globals.css";

export const metadata = {
  title: "Journey of Hope",
  description: "",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
